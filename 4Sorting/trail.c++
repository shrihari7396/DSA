#include <bits/stdc++.h>
using namespace std;

vector<int> insertion_sort(vector<int> a,){
    int size = a.size();
    for(int i = 0; i < k; i++){
        int j = i;
        while(j > 0 && a[j - 1] < a[j]){
            int temp = a[j] ;
            a[j] = a[j-1];
            a[j-1] = temp;
            j--;
        }
    }
    return a;
}

int main(void){
    int a; 
    cin >> a;
    cout M
    return 0;
}